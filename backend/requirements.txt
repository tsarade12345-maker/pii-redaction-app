# Minimal requirements for PII Redaction WebApp - Render Compatible
# Using only packages with pre-built wheels to avoid compilation issues

# Web Framework
Flask==3.0.2
flask-cors==5.0.1
Flask-SocketIO==5.5.1
gunicorn==21.2.0

# Image Processing
Pillow==10.4.0

# PDF Processing
pdf2image==1.17.0
fpdf==1.7.2

# OCR
pytesseract==0.3.13

# PII Detection - using latest compatible versions
# Note: presidio-analyzer may install spacy as a dependency, but models are NOT needed
presidio-analyzer==2.2.357

# Speech Recognition
SpeechRecognition==3.14.1

# Core Dependencies
numpy==1.26.4
python-engineio==4.11.2
python-socketio==5.12.1
Werkzeug==3.0.1
Jinja2==3.1.3
MarkupSafe==2.1.5
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
certifi==2023.11.17
charset-normalizer==3.3.2
idna==3.4
urllib3==2.1.0

# Presidio dependencies
pydantic==2.11.2
typing_extensions==4.12.2

# spaCy is NOT required - presidio-analyzer works without it
# The app will use default AnalyzerEngine if spaCy is not available
# DO NOT install spaCy models during build to avoid memory issues
