# Ultra-lightweight requirements for PII Redaction WebApp - Render Compatible
# Optimized to stay under 512MB memory limit

# Web Framework (minimal)
Flask==3.0.2
flask-cors==5.0.1
gunicorn==21.2.0

# Image Processing (lightweight)
Pillow==10.4.0

# PDF Processing (lightweight)
pdf2image==1.17.0
fpdf==1.7.2

# OCR (lightweight wrapper)
pytesseract==0.3.13

# Core Dependencies (minimal Flask stack)
Werkzeug==3.0.1
Jinja2==3.1.3
MarkupSafe==2.1.5
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# Utilities (minimal)
python-dotenv==1.0.0
requests==2.31.0

# Note: presidio-analyzer REMOVED to save memory
# PII detection now uses regex-only from utils/pii_detector.py
# Note: numpy REMOVED - only needed for optional cv2 preprocessing
# Note: All heavy ML/NLP libraries removed - using lightweight regex detection
